const GAME_CONFIG={
    ctxWidth : 500,
    ctxHeight : 872,
    winWidth : 1920,
    winHeight : 1080,
    unitSize : 50       //每一个小正方体的尺寸
}

Object.freeze(GAME_CONFIG);

module.exports=GAME_CONFIG;