{
  "name": "drawing",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "package": "electron-packager . Drawing  --platform=win32 --arch=x64 --icon=computer.ico --out=./out --asar --app-version=0.0.1 --overwrite --ignore=node_modules",
    "rebuildnew": "npm rebuild --runtime=electron --target=10.1.3 --disturl=https://atom.io/download/atom-shell --abi=83"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^10.1.3"
  },
  "iohook": {
    "targets": [
      "node-72",
      "electron-70"
    ],
    "platforms": [
      "win32",
      "darwin",
      "linux"
    ],
    "arches": [
      "x64",
      "ia32"
    ]
  },
  "dependencies": {
    "iohook": "^0.6.6"
  }
}
